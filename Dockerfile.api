# Dockerfile.api

FROM sicar-base:latest

WORKDIR /donwload-car

COPY app.py /donwload-car/

RUN pip install fastapi uvicorn httpx Pillow tqdm

ENTRYPOINT ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]

